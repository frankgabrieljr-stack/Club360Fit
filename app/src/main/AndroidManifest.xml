<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".Club360FitApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo_burgundy"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_burgundy"
        android:supportsRtl="true"
        android:theme="@style/Theme.Club360Fit">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Club360Fit">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link used for Supabase password reset: club360fit://reset -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="club360fit"
                    android:host="reset" />
            </intent-filter>
        </activity>
    </application>
</manifest>
